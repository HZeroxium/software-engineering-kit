# AI-Ready Context Block Example

```text
Repository: example-user-service

Purpose:
- Likely Java backend service for user profile RPC operations.
- Evidence: README service description, UserRpcService class, CI workflow.

Primary languages:
- Java: src/main/java, src/test/java, build.gradle.
- YAML: CI and deployment config.
- INI: environment configuration under conf/.

Application type:
- Backend service.
- No public framework confirmed from initial scan.

Architecture shape:
- Runtime bootstrap in com.acme.user.Main.
- RPC boundary under com.acme.user.rpc.
- Application services under com.acme.user.service.
- Data access under com.acme.user.repository.
- Internal platform wrappers used for config, RPC, logging, and metrics.

Build system:
- Gradle with wrapper.
- Evidence: settings.gradle, build.gradle, gradlew.

Validation commands:
- Build: candidate ./gradlew build.
- Test: candidate ./gradlew test.
- Full validation: confirmed ./gradlew check from CI.
- Local run: unknown.

Key directories:
- src/main/java: production Java code.
- src/test/java: Java tests.
- src/main/resources: runtime resources.
- conf: environment config.
- .github/workflows: CI workflow.

Internal libraries/custom framework signals:
- com.acme.platform.config: observed config loading.
- com.acme.platform.rpc: observed server bootstrap.
- com.acme.platform.metrics: observed metrics registration.
- Behavior must be inferred from existing usage and tests only.

Configuration:
- conf/development.ini, conf/staging.ini, conf/production.ini.
- Production config is high risk and should not be modified without approval.

CI/CD and deployment:
- .github/workflows/ci.yml runs ./gradlew check.
- Dockerfile exists.
- Deployment target unknown.

Generated code:
- build/generated appears generated by Gradle tasks.
- Manual edit policy: avoid.

Risk areas:
- AuthInterceptor.java: auth/security.
- conf/production.ini: production config.
- build.gradle and lockfiles: dependency/build behavior.
- build/generated/**: generated code.
- CI workflow: validation and deployment risk.

Known unknowns:
- Local run command.
- Internal RPC lifecycle behavior.
- Required credentials for integration tests.
- Deployment environment.

Repository-specific rules inferred from evidence:
- Prefer internal platform wrappers over raw RPC/config/metrics APIs.
- Validate with ./gradlew check before completion when feasible.
- Do not edit generated code manually.
- Do not modify production config without approval.

Safe next-step guidance for AI:
- Start read-only.
- Inspect relevant files before editing.
- Do not assume Spring or other frameworks.
- Do not invent internal APIs.
- Ask before modifying high-risk areas.
- Validate with confirmed commands before claiming completion.
```
